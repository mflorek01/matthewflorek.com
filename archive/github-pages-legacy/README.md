# Legacy GitHub Pages site

This folder preserves the pre-portfolio GitHub Pages site that previously lived on the `main` branch of `matthewflorek.com`.

Source snapshot: GitHub `main` commit `35966e6` (`Update Resume.pdf`), captured before the maintained portfolio replaced that branch.

It is retained for reference only. The maintained portfolio application is the repository root.
